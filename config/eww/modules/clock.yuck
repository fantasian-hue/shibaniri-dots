;;;yuck taken from linkfrg's git;;;;

(defwidget clock []
	(box :class {"clock " + theme} :orientation "vertical"
    	(label :class "clock-hour" :text {formattime(EWW_TIME, "%H")}
    )
        (label :class "clock-minute" :text {formattime(EWW_TIME, "%M")}
    ))
)

